setTimeout(function(param1, param2) {
  // Code to be executed after the delay, using the passed parameters
}, 3000, 'Hello', 'World'); // Delay of 3000 milliseconds (3 seconds) and passing 'Hello' and 'World' as parameters
